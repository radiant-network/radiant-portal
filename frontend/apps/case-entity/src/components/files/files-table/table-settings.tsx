import { createColumnSettings } from '@/components/base/data-table/data-table';

export function getCaseEnityFilesColumns(t: TFunction<string, undefined>) {
  return [];
}

export const defaultSettings = createColumnSettings([]);
