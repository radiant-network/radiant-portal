# filter-unsaved-modal

Confirmation alert displayed when the user tries to discard unsaved changes (`NewFilterButton`, `DuplicateFilterButton` when `isUnsaved`). 

![filter-unsaved-modal](../assets/filter-unsaved-modal.png)
