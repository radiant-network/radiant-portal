# Query Builder

Query-Builder is the composition of a [queries-bar-card](queries-bar-card.md) and [facet-list](facets/facet-list.md). 

Both component are means to works together. It should be the main component to use when adding what we call a `QueryBuilder`

![query-builder-layout](assets/query-builder-layout.png)