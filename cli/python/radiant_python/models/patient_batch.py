# coding: utf-8

"""
    Radiant API

    This is the API for Radiant data platform.

    The version of the OpenAPI document: 1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import date
from pydantic import BaseModel, ConfigDict, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class PatientBatch(BaseModel):
    """
    PatientBatch
    """ # noqa: E501
    date_of_birth: date
    first_name: Optional[StrictStr] = None
    jhn: Optional[StrictStr] = None
    last_name: Optional[StrictStr] = None
    life_status_code: StrictStr
    patient_organization_code: StrictStr
    sex_code: StrictStr
    submitter_patient_id: StrictStr
    submitter_patient_id_type: StrictStr
    __properties: ClassVar[List[str]] = ["date_of_birth", "first_name", "jhn", "last_name", "life_status_code", "patient_organization_code", "sex_code", "submitter_patient_id", "submitter_patient_id_type"]

    @field_validator('life_status_code')
    def life_status_code_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['alive', 'deceased', 'unknown']):
            raise ValueError("must be one of enum values ('alive', 'deceased', 'unknown')")
        return value

    @field_validator('sex_code')
    def sex_code_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['male', 'female', 'unknown']):
            raise ValueError("must be one of enum values ('male', 'female', 'unknown')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PatientBatch from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PatientBatch from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "date_of_birth": obj.get("date_of_birth"),
            "first_name": obj.get("first_name"),
            "jhn": obj.get("jhn"),
            "last_name": obj.get("last_name"),
            "life_status_code": obj.get("life_status_code"),
            "patient_organization_code": obj.get("patient_organization_code"),
            "sex_code": obj.get("sex_code"),
            "submitter_patient_id": obj.get("submitter_patient_id"),
            "submitter_patient_id_type": obj.get("submitter_patient_id_type")
        })
        return _obj


